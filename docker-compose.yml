version: '3.8'

services:
  lookupprotocol:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./database:/app/database
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped

